LET viol(id) =
  ( EXISTS a, b, label. ( P(id,"es",a,b,label) AND a >= 0 AND a <= 2))
  OR
  ( EXISTS a, b, label. ( P(id,"es",a,b,label) AND 3 < b ) )
IN
  viol(id)
  AND ONCE[1,*] ( viol(id) AND ONCE[1,*] viol(id) )
